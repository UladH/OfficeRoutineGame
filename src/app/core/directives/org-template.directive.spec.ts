import { OrgTemplateDirective } from './org-template.directive';

describe('OrgTemplateDirective', () => {
  it('should create an instance', () => {
    //const directive = new OrgTemplateDirective();
    //expect(directive).toBeTruthy();
  });
});
